{
  "name": "syariah-app-website",
  "version": "1.0.11",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "lint": "node scripts/validate-site.js",
    "build": "node scripts/validate-site.js",
    "test": "node scripts/validate-site.js",
    "verify": "npm run lint && npm run build && npm run test"
  }
}
